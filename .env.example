# Backend URLs are compiled-in constants (see Services/AppConfig.cs), not
# loaded from .env at runtime. This file is a reference for the values used
# there. Never commit real backend hostnames/IPs you consider private.

ApiUnencryptedUrl=http://YOUR_LAN_IP:7000/api/
ApiEncryptedUrl=https://your-backend-host.example.com/api/
